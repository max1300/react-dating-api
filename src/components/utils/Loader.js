import React from "react";
import {DoubleBounce} from "better-react-spinkit";

const Loader = () => {

    return (
            <DoubleBounce size={150} color={"black"}/>
    );
}

export default Loader;